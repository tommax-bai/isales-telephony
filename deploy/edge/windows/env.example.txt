# iSales Windows edge client environment file template.
#
# Spec: windows-client-core / deployment-topology § Scenario "Windows 安装路径约定".
#
# Real file lives at: %APPDATA%\isales\env\telephony.env
# install.ps1 seeds this template there on first run; the activation
# dialog (or `isales-telephony.exe`'s first launch) overwrites the
# placeholder values once the user pastes their token.
#
# Lines starting with `#` are comments; KEY=value pairs are read by
# main_windows.py at startup via `isales_telephony.ui.env_writer.read_env`.
# Values containing spaces / special characters may be quoted with
# double quotes.

# ----------------------------------------------------------------------
# Activation: paste the EDGE_DEVICE_TOKEN here OR leave blank and use
# the activation dialog. The token is signed by cloud isales-api.
# ----------------------------------------------------------------------
ISALES_EDGE_DEVICE_TOKEN=

# Cloud-edge gRPC endpoint. Override only if the operator has provisioned
# a region-specific endpoint; the default is the production isales LB.
ISALES_CLOUD_GRPC_ENDPOINT=isales.example.com:443

# ----------------------------------------------------------------------
# Optional knobs — sensible defaults; uncomment to override.
# ----------------------------------------------------------------------

# Audio backend. Auto-detected from sys.platform; the env var below is
# only honoured by `isales-telephony-edge` (the headless entrypoint).
# main_windows.py ignores it and always uses WASAPI on Windows.
# ISALES_EDGE_AUDIO_BACKEND=windows

# WASAPI mode. "shared" (default) keeps the modem audio device available
# to other apps; "exclusive" gives the lowest latency at the cost of
# muting other audio on the same device.
# ISALES_WASAPI_MODE=shared

# Logging verbosity. DEBUG | INFO | WARNING | ERROR. INFO is default.
# ISALES_LOG_LEVEL=INFO

# Edge event buffer path. Defaults to
#   %APPDATA%\isales\sqlite\edge_buffer.db
# Override only if you store user data on a different drive.
# ISALES_EDGE_EVENT_BUFFER_PATH=

# ----------------------------------------------------------------------
# Reserved for D2 / D3 (commented out — do not enable in D1):
# ----------------------------------------------------------------------
# ISALES_OTA_CHANNEL=stable           # D3 windows-installer-and-ota
# ISALES_HEALTH_REPORT_INTERVAL_S=60  # D2 hardware-observability
